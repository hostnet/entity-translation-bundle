hostnet:
    bundle:
        entity_translation_bundle:
            functional:
                fixtures:
                    enum:
                        reply_status:
                            open: open
                            closed: closed
                        post_status:
                            hidden: "Hidden (vendor file)"
                            visibile: "Visible (vendor file, is overridden)"
